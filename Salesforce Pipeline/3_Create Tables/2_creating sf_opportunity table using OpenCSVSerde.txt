-- part of salesforce pipeline
-- creates table using data from salesforce opportunity object

CREATE EXTERNAL TABLE sf_opportunity (
  `row_id` string,
  `id` string,
  `isdeleted` string,
  `accountid` string,
  `recordtypeid` string,
  `name` string,
  `stagename` string,
  `amount` string,
  `probability` string,
  `closedate` string,
  `type` string,
  `nextstep` string,
  `leadsouce` string,
  `isclosed` string,
  `iswon` string,
  `forecastcategory` string,
  `forecastcategoryname` string,
  `campaignid` string,
  `hasopportunitylineitem` string,
  `pricebook2id` string,
  `ownerid` string,
  `createddate` string,
  `createdbyid` string,
  `lastmodifieddate` string,
  `lastmodifiedbyid` string,
  `systemmodstamp` string,
  `lastactivitydate` string,
  `fiscalquarter` string,
  `fiscalyear` string,
  `fiscal` string,
  `contactid` string,
  `lastvieweddate` string,
  `lastreferenceddate` string,
  `hasopenactivity` string,
  `hasoverduetask` string,
  `practices__c` string,
  `physicians__c` string,
  `program__c` string,
  `amount_received__c` string,
  `invoicing_cycle__c` string,
  `monthly_amount__c` string,
  `contract_version__c` string,
  `contract_execution_date__c` string,
  `implementation_start_date__c` string,
  `expiration_date__c` string,
  `retire_date__c` string,
  `funder__c` string,
  `hope_grant_funder__c` string,
  `expected_revenue__c` string,
  `allowance_fulfilled__c` string,
  `selling_diabetic_supplies__c` string,
  `date_became_closed__c` string,
  `renewal_status__c` string,
  `first_payment_received__c` string,
  `old_stage_value__c` string,
  `sales_cycle_communications__c` string,
  `last_stage_before_unsuccessful_prospect__c` string,
  `account_record_type__c` string,
  `renewal_amount__c` string,
  `date_became_active__c` string,
  `opp_safe_id__c` string,
  `contract_report_date__c` string,
  `date_became_unsuccesful_prospect__c` string,
  `date_in_awareness__c` string,
  `date_in_engaged__c` string,
  `date_in_convert__c` string,
  `date_in_closed__c` string,
  `date_in_on_hold_sales__c` string,
  `days_in_awareness__c` string,
  `days_in_engaged__c` string,
  `days_in_convert__c` string,
  `days_in_closed__c` string,
  `days_in_awareness_before_on_hold_sales__c` string,
  `days_in_engaged_before_on_hold_sales__c` string,
  `days_in_convert_before_on_hold_sales__c` string,
  `days_in_on_hold_sales__c` string,
  `days_from_awareness_to_closed__c` string,
  `date_out_on_hold_sales__c` string,
  `awareness_current_start__c` string,
  `awareness_total_duration_secs__c` string,
  `hold_current_start__c` string,
  `hold_total_duration_secs__c` string,
  `engaged_current_start__c` string,
  `convert_current_start__c` string,
  `closed_current_start__c` string,
  `engaged_total_duration_secs__c` string,
  `convert_total_duration_secs__c` string,
  `closed_total_duration_secs__c` string,
  `awareness_total_duration__c` string,
  `engaged_total_duration__c` string,
  `convert_total_duration__c` string,
  `closed_total_duration__c` string,
  `hold_total_duration__c` string,
  `grant_start_date__c` string,
  `grant_end_date__c` string,
  `supplement_fpl__c` string,
  `joinder_agreement__c` string,
  `lilly_notified__c` string,
  `contracting_comments__c` string,
  `med_request__c` string,
  `lilly_call_center__c` string,
  `lilly_call_center_no_reason__c` string,
  `opportunity_record_type__c` string,
  `is_mine__c` string 
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.OpenCSVSerde'
WITH SERDEPROPERTIES (
  'separatorChar' = ',',
  'quoteChar' = '\"',
  'escapeChar' = '\n'
) LOCATION 's3://salesforceobjectexport/Opportunity_clean/'
TBLPROPERTIES ('has_encrypted_data'='false',
               'skip.header.line.count' = '1');